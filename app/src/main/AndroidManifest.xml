<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name="com.stPupero.cryptocurrencyapp.CoinApplication"
        android:allowBackup="true"
        android:icon="@mipmap/coin_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/coin_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CryptocurrencyApp">
        <activity
            android:name="com.stPupero.cryptocurrencyapp.presentation.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CryptocurrencyApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>